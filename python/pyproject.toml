[project]
name = "agentic-aegis"
version = "0.1.0"
description = "Python bindings for AgenticAegis streaming validation"
license = "MIT"
requires-python = ">=3.8"

[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"
